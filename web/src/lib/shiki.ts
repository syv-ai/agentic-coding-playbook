/** One pair of highlighter themes for fenced blocks (astro.config) and <Code> (CodeSteps.astro). global.css switches them. */
export const SHIKI_THEMES = { light: "github-light", dark: "github-dark" } as const;
